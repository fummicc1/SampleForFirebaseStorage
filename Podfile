# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SampleForFirebaseStorage' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SampleForFirebaseStorage
pod 'Firebase/Storage'
pod 'Firebase/Firestore'
pod 'Firebase/Core'

  target 'SampleForFirebaseStorageTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SampleForFirebaseStorageUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
